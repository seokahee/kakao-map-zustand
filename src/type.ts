export type StorePositionsType = {
  id: string;
  storeName: string;
  machine: string;
  lat: string | number;
  lng: string | number;
  address: string;
};

// 그 외
// 드롭슬라이드 메뉴
// 드롭시 삭제버튼이 오른쪽에 나오게

// 완료 목록
// - 마커 클러스터 적용
