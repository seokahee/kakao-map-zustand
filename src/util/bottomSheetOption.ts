export const MIN_Y = window.innerHeight * 0.1;
export const MAX_Y = window.innerHeight * 1 - 50;
export const MAX_Y_SHEET_HEIGHT = window.innerHeight - MIN_Y;

// MIN_Y 바텀 시트가 최대로 올라갔을 때 Y좌표 값
// MAX_Y 바컴 시트가 최대로 내려갔을 때 Y좌표 값
// MAX_Y_SHEET_HEIGHT 바텀시트 높이
